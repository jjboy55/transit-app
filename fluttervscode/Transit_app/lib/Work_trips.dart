import 'package:flutter/material.dart';

class WorkTrips extends StatelessWidget {
  const WorkTrips({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
